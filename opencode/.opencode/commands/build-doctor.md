---
description: Triple Threat install diagnostic — audits install state and surfaces fix commands; read-only
agent: build
---

Load the `build-doctor` skill with the Skill tool and execute it as the Triple Threat `/build-doctor` diagnostic.

User arguments:
`$ARGUMENTS`

If the skill is not available, stop and report that `build-doctor` is not installed at `.opencode/skills/build-doctor/SKILL.md` or `~/.config/opencode/skills/build-doctor/SKILL.md`. Do not replace the diagnostic with an ad hoc implementation.
