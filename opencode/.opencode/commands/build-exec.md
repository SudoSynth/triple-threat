---
description: Execute a Triple Threat plan with TDD discipline
agent: build
---

Load the `build-exec` skill with the Skill tool and execute it as the Triple Threat `/build-exec` orchestrator.

User arguments:
`$ARGUMENTS`

If the skill is not available, stop and report that `build-exec` is not installed at `.opencode/skills/build-exec/SKILL.md` or `~/.config/opencode/skills/build-exec/SKILL.md`.
