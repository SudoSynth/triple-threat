---
description: Bootstrap Triple Threat workspace and codebase map
agent: build
---

Load the `build-init` skill with the Skill tool and execute it as the Triple Threat `/build-init` orchestrator.

User arguments:
`$ARGUMENTS`

If the skill is not available, stop and report that `build-init` is not installed at `.opencode/skills/build-init/SKILL.md` or `~/.config/opencode/skills/build-init/SKILL.md`.
