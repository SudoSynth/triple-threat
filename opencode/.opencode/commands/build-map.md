---
description: Run or refresh the Triple Threat codebase map
agent: build
---

Load the `build-map` skill with the Skill tool and execute it as the Triple Threat `/build-map` orchestrator.

User arguments:
`$ARGUMENTS`

If the skill is not available, stop and report that `build-map` is not installed at `.opencode/skills/build-map/SKILL.md` or `~/.config/opencode/skills/build-map/SKILL.md`.
