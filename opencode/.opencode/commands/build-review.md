---
description: Run Triple Threat multi-pass code review
agent: build
---

Load the `build-review` skill with the Skill tool and execute it as the Triple Threat `/build-review` orchestrator.

User arguments:
`$ARGUMENTS`

If the skill is not available, stop and report that `build-review` is not installed at `.opencode/skills/build-review/SKILL.md` or `~/.config/opencode/skills/build-review/SKILL.md`.
