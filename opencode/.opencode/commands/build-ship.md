---
description: Prepare a Triple Threat branch for PR shipping
agent: build
---

Load the `build-ship` skill with the Skill tool and execute it as the Triple Threat `/build-ship` orchestrator.

User arguments:
`$ARGUMENTS`

If the skill is not available, stop and report that `build-ship` is not installed at `.opencode/skills/build-ship/SKILL.md` or `~/.config/opencode/skills/build-ship/SKILL.md`.
