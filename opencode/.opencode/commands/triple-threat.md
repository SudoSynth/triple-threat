---
description: Show the Triple Threat pipeline reference
agent: build
---

Load the `triple-threat` skill with the Skill tool, then present its Triple Threat reference: architecture, command surface, state spine, and when to use each `/build-*` command.

User arguments:
`$ARGUMENTS`

If the skill is not available, stop and report that `triple-threat` is not installed at `.opencode/skills/triple-threat/SKILL.md` or `~/.config/opencode/skills/triple-threat/SKILL.md`.
