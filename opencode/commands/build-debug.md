---
description: Triple Threat explicit debugging entry — investigate first, ask before continuing
agent: build
---

Load the `build-debug` skill with the Skill tool and execute it as the Triple Threat `/build-debug` orchestrator.

User arguments:
`$ARGUMENTS`

If the skill is not available, stop and report that `build-debug` is not installed at `.opencode/skills/build-debug/SKILL.md` or `~/.config/opencode/skills/build-debug/SKILL.md`. Do not replace the orchestrator with an ad hoc implementation.
