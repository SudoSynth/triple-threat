---
description: Pressure-test and plan a Triple Threat feature
agent: build
---

Load the `build-plan` skill with the Skill tool and execute it as the Triple Threat `/build-plan` orchestrator.

User arguments:
`$ARGUMENTS`

If the skill is not available, stop and report that `build-plan` is not installed at `.opencode/skills/build-plan/SKILL.md` or `~/.config/opencode/skills/build-plan/SKILL.md`.
