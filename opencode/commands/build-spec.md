---
description: Write a Triple Threat feature spec
agent: build
---

Load the `build-spec` skill with the Skill tool and execute it as the Triple Threat `/build-spec` orchestrator.

User arguments:
`$ARGUMENTS`

If the skill is not available, stop and report that `build-spec` is not installed at `.opencode/skills/build-spec/SKILL.md` or `~/.config/opencode/skills/build-spec/SKILL.md`.
